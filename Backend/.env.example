# EliteProAI Backend – Environment Variables
# Copy to .env and fill in real values for each environment.

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=eliteproai
DB_PASSWORD=password
DB_NAME=eliteproai_dev

# JWT
JWT_SECRET=replace-with-a-secure-random-string-in-production

# Server
PORT=8080

# Email (for password resets — configure when ready)
# SENDGRID_API_KEY=
# FROM_EMAIL=noreply@eliteproai.com
